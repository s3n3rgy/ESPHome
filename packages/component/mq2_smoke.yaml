sensor:
  #MQ-2 Smoke sensor. Above 30% is bad.
  - platform: adc
    pin: GPIO32
    name: "Smoke sensor"
    id: MQ2
    update_interval: 1s
    filters:
      - multiply: 100
    unit_of_measurement: "%"
    icon: "mdi:percent"
